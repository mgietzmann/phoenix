# Questions

## New

- What MCP server design pattern is the right fit for giving the agent context about available data sources (tables, spatial coverage, temporal coverage)?
- What visualization library or framework should the web app use to render spatial figures (globes, H3 overlays, tracks)?
- What database is the backend — PostgreSQL with PostGIS? Something else?
- How should credential persistence work in practice — written to a mounted volume? Encrypted? Plain config file?
- What does the chat UI framework look like — build from scratch, use an existing open-source chat component, or something else?
- Should the agent be able to generate figures iteratively (refine a visualization through follow-up prompts) or is each request standalone?

## Developed

## Answered
